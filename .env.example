# Example environment configuration for letterboxd-recommender

# CORS: comma-separated or newline-separated list of allowed origins.
# Use "*" only for quick demos.
LETTERBOXD_RECOMMENDER_CORS_ORIGINS=http://localhost:8000

# Data dir (ingested RSS cache, derived dataframe cache, and default session DB location)
LETTERBOXD_RECOMMENDER_DATA_DIR=data

# Optional explicit session DB path
# LETTERBOXD_RECOMMENDER_SESSION_DB=data/sessions.sqlite3

# Rate limiting
LETTERBOXD_RECOMMENDER_RL_GLOBAL=60
LETTERBOXD_RECOMMENDER_RL_GLOBAL_WINDOW_S=60
LETTERBOXD_RECOMMENDER_RL_INGEST=5
LETTERBOXD_RECOMMENDER_RL_INGEST_WINDOW_S=60
